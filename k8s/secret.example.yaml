# Example Secret — copy to secret.yaml, fill values, apply: kubectl apply -f secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: tool-service-secrets
type: Opaque
stringData:
  database-url: "postgres://user:pass@postgres:5432/tool?sslmode=disable"
  wordstat-api-key: ""
  wordstat-folder-id: ""
  currency-api-key: ""
